$("#ajax-comments h2").html("<%= @comments.count %><%= ' ' + 'Comment'.pluralize(@comments.count) %>")
$("#comments-list").empty()
  .append("<%= j render partial: 'comment', collection: @comments %>")
